<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Bulletin de Notes</title>
    <style>
        body { font-family: Arial, sans-serif; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .header { text-align: center; margin-bottom: 20px; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Bulletin de Notes</h1>
        <p>Année Académique: {{ anneeAcademique }}</p>
        <p>Nom de l'Étudiant: {{ etudiant.nom }}</p>
    </div>
    <table>
        <thead>
            <tr>
                <th>Cours</th>
                <th>Crédits</th>
                <th>Note</th>
            </tr>
        </thead>
        <tbody>
            {% for note in notes %}
                <tr>
                    <td>{{ note.cours ? note.cours.nom : 'Cours non défini' }}</td>
                    <td>{{ note.cours ? note.cours.credits : 'credits non défini' }}</td>
                    <td>{{ note.note }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
    <p>Moyenne Générale: {{ moyenneGenerale }}</p>
</body>
</html>
